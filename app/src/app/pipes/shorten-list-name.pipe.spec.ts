import { ShortenListNamePipe } from './shorten-list-name.pipe';

describe('ShortenListNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenListNamePipe();
    expect(pipe).toBeTruthy();
  });
});
